# Copy to `.env` for local development. Do NOT commit `.env`.

# Buildatics Platform API base URL (server-only; consumed by the BFF in src/features/auth/lib/api-client.ts).
# No NEXT_PUBLIC_ prefix on purpose — the browser must never see the API or hold tokens.
BUILDATICS_API_BASE_URL=https://bap-dev-mon-cap-api.orangeflower-bf81320d.australiaeast.azurecontainerapps.io
